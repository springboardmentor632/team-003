-- Create the decisionhub_db database for local development
CREATE DATABASE IF NOT EXISTS decisionhub_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
-- Example: create a dedicated user (adjust as needed)
-- CREATE USER 'decisionhub_user'@'localhost' IDENTIFIED BY 'change_me';
-- GRANT ALL PRIVILEGES ON decisionhub_db.* TO 'decisionhub_user'@'localhost';
-- FLUSH PRIVILEGES;
